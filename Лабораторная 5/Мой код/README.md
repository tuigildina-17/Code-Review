ОТЧЁТ ПО КОД-РЕВЬЮ МОЕЙ ЛАБОРАТОРНОЙ
- Исправленные ошибки:
1. Устранение табуляции
- Табуляции в коде были заменены на стандартные пробелы, что привело стиль форматирования к единому виду.
2. Добавление Javadoc‑комментариев
- Для всех методов и ключевых классов были добавлены корректные Javadoc‑комментарии, описывающие назначение, параметры и возвращаемые значения.
3. Исправлены строки, превышающие 100 символов
- Длинные строки были разбиты на несколько более коротких, что соответствует стандарту Google Java Style и улучшает восприятие кода.
4. Реализована корректная проверка при добавлении дороги между городами
- Первоначальный код был заменён на расширенную и безопасную логику:
- - if (fromCity == null || toCity == null) {
- -  System.out.println("Город не найден.");
- - }
- - else if (fromCity == toCity) {
- -   System.out.println("Нельзя добавить дорогу в тот же самый город.");
- - }
- - else if (fromCity.getRoads().containsKey(toCity)) {
- -   System.out.println("Дорога уже существует. Стоимость обновлена.");
- -   fromCity.addRoad(toCity, cost);
- - }
- - else {
- -   fromCity.addRoad(toCity, cost);
- - }
5. Улучшена читаемость параметров метода addRoad()
- Имя параметра to заменено на такое, что отражает его смысл (destination)
6. Добавлены фигурные скобки для однострочных операторов
- Все конструкции if, else, for, while теперь используют фигурные скобки, даже если содержат одну строку.
