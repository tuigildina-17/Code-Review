ОТЧЁТ ПО ЛАБОРАТОРНОЙ
ЗАДАНИЕ 1.
--------------------------------------- 
**Alice.yandex.ru:**
1. Google Style запрещает использование using namespace std;
- Исправление: Все обращения заменены на std::cout, std::string, std::getline и т.д.
2. Функция называлась toMorseCode, что нарушает стиль snake_case.
- Исправление: Переименована в to_morse_code
3. Комментарии вроде // Приводим символ к верхнему регистру не добавляли смысла
- Исправление: Удалены
4. Google Style запрещает глобальные переменные.
- Исправление: Создана функция get_morse_map(), возвращающая static const словарь.
5. Имя переменной не соответствовало стилю констант
- Исправление: Переименована в kMorseCode
6. Отсутствие проверки корректности ввода
- Исправление: Добавлена функция is_valid_input
7. Неверная работа с кодировкой консоли
- Исправление: Добавлены строки: SetConsoleCP(1251); SetConsoleOutputCP(1251); setlocale(LC_ALL, "RU");
8. Некоторые строки превышали 80 символов
- Исправление: Разбиение строки на несколько строк
-----------------------------------------------------------------
  **Copilot.microsoft.com:**
1. Комментарии вроде // Приводим символ к верхнему регистру не добавляли смысла
- Исправление: Удалены
2. Google Style запрещает глобальные переменные.
- Исправление: Создана функция get_morse_map(), возвращающая static const словарь.
3. Имя переменной не соответствовало стилю констант
- Исправление: Переименована в kMorseCode
4. Отсутствие проверки корректности ввода
- Исправление: Добавлена функция is_valid_input
5. Неверная работа с кодировкой консоли
- Исправление: Добавлены строки: SetConsoleCP(1251); SetConsoleOutputCP(1251); setlocale(LC_ALL, "RU");
6. Некоторые строки превышали 80 символов
- Исправление: Разбиение строки на несколько строк
------------------------------------------------------------
  **Chat.deepseek.com:**
1. Google Style запрещает использование using namespace std;
- Исправление: Все обращения заменены на std::cout, std::string, std::getline и т.д.
2. Комментарии вроде // Приводим символ к верхнему регистру не добавляли смысла
- Исправление: Удалены
3. Google Style запрещает глобальные переменные.
- Исправление: Создана функция get_morse_map(), возвращающая static const словарь.
4. Имя переменной не соответствовало стилю констант
- Исправление: Переименована в kMorseCode
5. Отсутствие проверки корректности ввода
- Исправление: Добавлена функция is_valid_input
6. Неверная работа с кодировкой консоли
- Исправление: Добавлены строки: SetConsoleCP(1251); SetConsoleOutputCP(1251); setlocale(LC_ALL, "RU");
7. Некоторые строки превышали 80 символов
- Исправление: Разбиение строки на несколько строк
