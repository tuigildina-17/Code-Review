ОТЧЁТ ПО ЛАБОРАТОРНОЙ
- ЗАДАНИЕ 1
1. Google Style запрещает использование using namespace std;
- Исправление: Все обращения заменены на std::cout, std::string и т.д.
2. Использовалась табуляция
- Исправление: Все отступы заменены на 2 пробела
3. Имена класса, полей и переменных не соответствовали Google Style
- Исправление: Переменные переименованы (int d -> int data, Node(int val)	-> Node(int value)), поля переименованы (Node* p1 -> Node* top_, int cnt	-> int count_), класс переименован (class stack	-> class Stack)
4. Несодержательные комментарии (Google Style рекомендует писать комментарии, объясняющие почему, а не что)
- Исправление: Комментарии удалены  
5. Отсутствие проверки корректности ввода
- Исправление: Добавлена проверка: while (true) {
  std::cin >> n;
  if (std::cin.fail() || n <= 0) {
    std::cin.clear();
    std::cin.ignore(10000, '\n');
    std::cout << "Ошибка! Введите положительное число." << std::endl;
    continue;
  }
  break;
}
------------------------------------------------------------------
- ЗАДАНИЕ 2
1. Google Style запрещает использование using namespace std;
- Исправление: Все обращения заменены на std::cout, std::string и т.д.
2. Использовалась табуляция
- Исправление: Все отступы заменены на 2 пробела
3. Имена функции, методов, полей и переменных не соответствовали Google Style
- Исправление: Функция переименована (IsNumber -> is_number), переменные переименованы (std::string input_num;), методы переименованы (Node* getP1() const; -> Node* get_p1() const;), поля переименованы (Node* P1; -> Node* p1_;)
4. Несодержательные комментарии (Google Style рекомендует писать комментарии, объясняющие почему, а не что)
- Исправление: Комментарии удалены
-----------------------------------------------------------------------
- ЗАДАНИЕ 3
1. Google Style запрещает использование using namespace std;
- Исправление: Все обращения заменены на std::cout, std::string и т.д.
2. Использовалась табуляция
- Исправление: Все отступы заменены на 2 пробела
3. Имена функций, методов и переменных не соответствовали Google Style
- Исправление: Функции переименованы (insertBeforeEverySecond -> insert_before_every_second), переменные переименованы (newNode -> new_node), методы переименованы (void clearList(); -> void clear_list();)
4. Несодержательные комментарии (Google Style рекомендует писать комментарии, объясняющие почему, а не что)
- Исправление: Комментарии удалены
5. Отсутствие кодировки ввода консоли для работы с русскими буквами
- Исправление: Добавлено setlocale(LC_ALL, "RU");
6. Отсутствие проверка корректности ввода
- Исправление: Добавлена функция: int read_int(const char* prompt);
-----------------------------------------------------
- ЗАДАНИЕ 4
1. Google Style запрещает использование using namespace std;
- Исправление: Все обращения заменены на std::cout, std::string и т.д.
2. Использовалась табуляция
- Исправление: Все отступы заменены на 2 пробела
3. Имена методов и переменных не соответствовали Google Style
- Исправление: Переменные переименованы (PerviyElement ->	first_element, Znachenie	-> value...), методы переименованы (Ochistka() ->	clear(), DobavitElement() ->	add_element()...), поля переименованы (Uzel ->	Node, Spisok	-> List)
4. Несодержательные комментарии (Google Style рекомендует писать комментарии, объясняющие почему, а не что)
- Исправление: Комментарии удалены




